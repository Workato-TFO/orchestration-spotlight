name: publish-guard

on:
  push:
  pull_request:

jobs:
  publish-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run pre-publish checks
        run: bash scripts/publish-checks.sh
